name: media-service.yml

on:
  push:
    branches: [ "main" ]
    paths:
      - 'message-persistence-service/**' # ONLY run this pipeline if files in the user-service folder change
      - 'common/**'
      - '.github/workflows/message-persistence-service.yml' # ALSO run if this specific workflow file is updated
  pull_request:
    branches: [ "main" ]
    paths:
      - 'message-persistence-service/**'
      - 'common/**'

# Required for AWS OIDC authentication
permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    name: Build & Deploy
    if: github.ref == 'refs/heads/main'
    defaults:
      run:
        working-directory: ./message-persistence-service

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup JDK
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '25'
          distribution: 'corretto'

      - name: Cache Maven Dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('message-persistence-service/pom.xml', 'common/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: Build and Install Common Module # Needed in this job as well
        working-directory: ./common
        run: ../mvnw clean install -DskipTests -B


      # --- AWS ECR --- #

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with: # Using OpenID Connect (OIDC) to assume a role
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: eu-south-2

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and Push image using Google Jib
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: chat-app/message-persistence-service
          IMAGE_TAG: ${{ github.sha }}
        #dynamically overrides what is written in your pom.xml
        run: |
          ../mvnw compile jib:build -Djib.to.image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
