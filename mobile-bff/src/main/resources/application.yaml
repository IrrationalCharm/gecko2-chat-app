server:
  port: 8087

spring:
  application:
    name: mobile-bff
  config:
    import: optional:configserver:http://localhost:8888
  threads:
    virtual:
      enabled: true
  mvc:
    servlet:
      path: "/mobile-bff"
